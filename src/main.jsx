import React from "react";
import ReactDOM from "react-dom/client";
import PackingSystem from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PackingSystem />
  </React.StrictMode>
);
